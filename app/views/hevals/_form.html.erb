<%= form_for(heval) do |f| %>
  <% if heval.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(heval.errors.count, "error") %> prohibited this heval from being saved:</h2>

      <ul>
      <% heval.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
