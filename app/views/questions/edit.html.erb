<h1>Editing Question</h1>
<%= form_for [@category,@question], url: category_question_path do |f| %>
    <%= render 'form', f: f, question: @question %>
<% end %>


<%= link_to 'Back', category_question_path(params[:category_id],params[:id]) %>
